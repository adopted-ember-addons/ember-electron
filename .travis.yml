
language: node_js

sudo: false

node_js:
  - "4.2"
  - "5.0"

before_install:
  - "npm config set spin false"

install:
  - npm install electron-rebuild
  - npm install --no-optional

script:
  - npm test
